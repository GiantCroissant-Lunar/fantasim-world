# UnifyGeometry.Operations

Engine-agnostic geometry algorithms for `fantasim-world`.

- Pure operations over `UnifyGeometry` primitives.
- No rendering / engine dependencies.
- Designed for derived products (meshes, charts, overlays) built in host apps.
